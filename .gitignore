# Local Claude Code / editor config (machine-specific, not part of the product)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~
.vscode/
.idea/

# Dependencies / build output (not used today; future-proofing)
node_modules/
dist/
build/

# ReferenceGuide is an independent, separately-initialized git repo nested here
ReferenceGuide/

# Rebrand source files (GIMP working docs, raw SVG/PNG exports) — large,
# not needed to serve the site. Final processed assets are committed
# directly at the repo root (readiness-arcade-logo.png, etc.).
Artwork/
